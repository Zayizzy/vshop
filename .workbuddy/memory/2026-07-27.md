# 2026-07-27

## Git 同步
- 从 `origin/main` (https://github.com/Zayizzy/vshop.git) 拉取代码同步本地
- 远端无新提交，本地已 up to date
- 拉取前用 `git stash -u` 保护本地未提交改动，拉取后 `git stash pop` 恢复，无冲突
- 恢复后本地改动：`.workbuddy/memory/MEMORY.md`、`miniprogram/app.js`、`miniprogram/config/index.js` 及若干未跟踪文件
- 注意：原本 staged 的 Dockerfile / schema.prisma / start.sh 在 pop 后不再出现在改动列表，若需找回可查 git reflog

## 商品图片改为云开发对象存储（v2）
- v1 用 cos-nodejs-sdk-v5 需 SecretId/Key；改用 `@cloudbase/node-sdk`
- `cos.service.ts` 重写：tcb.init({env}) 自动鉴权，对接云托管同账号桶 `7072-prod-d8gf4sglmae440765-1452085588`（ap-shanghai）
- 上传后 `getTempFileURL` 拿 10 年有效 https URL 返回
- 卸载 `cos-nodejs-sdk-v5`，安装 `@cloudbase/node-sdk@2.13.0`
- `env.validation.ts`：移除 4 个 COS 变量，改 `TCB_ENV_ID`（默认 `1452085588`）
- 部署文档 `docs/deploy-wx-cloudrun.md` 11.2 节重写反映实际方案
- nest build 通过。生产**零密钥配置**（云托管自动鉴权）

## COS 方案改为 STS 临时凭证（v3）
- v2 用 @cloudbase/node-sdk 在云托管容器内 init({env}) 失败（云开发 SDK 不适配云托管），日志显示上传 6ms = 走本地 fallback，多实例拿不到
- 改用 cos-nodejs-sdk-v5 + 云托管元数据 STS（`metadata.tencentyun.com/latest/meta-data/cam/security-credentials/<role>`）
- 上传后 cos.getObjectUrl 拿 10 年签名 URL
- CAM 给服务运行角色授对象存储读写权限（一次，零密钥）
- 卸载 @cloudbase/node-sdk（50 包），装回 cos-nodejs-sdk-v5（43 包）
- env.validation.ts: 4 个 COS_SECRET 改为 COS_BUCKET/REGION/ROLE_NAME（均有默认）
- 部署文档 11.2 重写

## 商品图片 COS 对象存储改造（修复云托管裂图）
- 问题：体验版/正式版（云托管）商品图片裂图不显示
- 根因：容器无状态 + 多实例文件不共享，`public/uploads/` 本地存储上传后路由到别的实例读不到 → 404
- 改造：新建 `vshop-server/src/modules/upload/cos.service.ts`（CosService，双模式）
  - 配置 COS_SECRET_ID/KEY/BUCKET 时 → 上传 COS 返回完整 https URL
  - 未配置时 → 回退本地磁盘存储（开发环境零配置可用）
- `upload.controller.ts` 改用 `memoryStorage()`，注入 `CosService.store()` 统一处理
- `upload.module.ts` 注册 CosService；`env.validation.ts` 增加 4 个 COS 可选变量
- 安装 `cos-nodejs-sdk-v5@^3.0.0`（dependencies）
- 前端**无需改动**：`utils/image.full()` 已兼容 https（原样返回）与相对路径（补全）
- `npm run build` 通过；`docs/deploy-wx-cloudrun.md` 第 11 节已标注实施
- 后续：生产在云托管控制台配 4 个 COS 环境变量即生效；历史相对路径数据无需迁移
