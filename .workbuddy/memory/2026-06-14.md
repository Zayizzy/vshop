# 2026-06-14 工作日志

## 生鲜商城小程序 - 产品规划
- 完成 PRD v1.0 文档（`docs/PRD.md`），覆盖 C端/分销/供应商/运营后台/订单路由/WMS 六大模块
- 完成页面路由表（`docs/pages-routing.md`），共 34 个页面，含 5 个 Tab、分包策略、版本映射、跳转关系图
- 完成数据模型设计（`docs/data-model.md`），覆盖 8 大体系、20+ 张表、关键设计决策
- 完成接口清单（`docs/api-spec.md`），覆盖 12 模块 41 接口，含版本映射与通用规范
- 确定商业模式三支柱：包裹卡片引流 + KOC社交分销 | 次日达供应商直发 | 多供应链+WMS对接
- 完成设计系统定义 + 9个页面原型：首页/分类(果蔬)/商品详情/购物车(拆包)/确认订单/订单列表/我的(KOC入口)/KOC工作台，完整覆盖5个Tab页+C端核心链路+分销体系
- 生成Ardot导入文件（`docs/ardot-import.json`），含30+变量、9页完整section结构、styleGuide搜索参数
- 完成小程序工程搭建：24个页面完整代码（5 Tab + 商品/下单/订单售后/KOC分销），含主包+4分包+API层+全局主题样式+渠道追踪
- 修复 WXSS 样式：5 个 Tab 页（home/category/cart/order/mine）全部改为硬编码 CSS，零 CSS 变量依赖，零缺失图片引用
- 修复 API 超时问题：app.js 添加 10s timeout；home/index.js 添加 mock 数据降级（banners/categories/recommendList）；order/list.js 添加 mock 订单数据；cart 和 category 已有内置 mock
- 添加全局 mockMode：app.js 新增 `mockMode: true` 开关；request() 立即 reject 零延迟；login() 返回假 token；onLaunch/onShow 跳过渠道上报。部署后端时只需将 mockMode 改为 false 即可恢复真实 API 调用
- 生成 64 张彩色 placeholder PNG（200×200），覆盖全部 mock 数据引用的商品图片、促销横幅、头像等，解决 /assets/images/ HTTP 500 错误
- TDesign UI 美化：安装 tdesign-miniprogram v1.8.0，构建 npm，移除 app.json style:v2，全局注册 t-icon/t-button/t-tag/t-image/t-toast
  - 首页：t-search 搜索入口 + t-tag 商品标签 + t-image 商品图 + t-icon 图标，新增 emoji 分类入口 + 渐变 Banner 指示器
  - 分类页：t-image 替代原生 image + t-icon 空状态
  - 购物车：t-stepper 数量控件 + t-progress 免邮进度 + t-image 商品图 + t-empty 空状态 + t-button 结算/删除按钮
  - 订单列表：t-tabs 状态标签 + t-tag 状态徽章 + t-image 商品图 + t-empty + t-button 操作按钮
  - 我的页：**完整重写**（修复 WXML/WXSS 类名全不匹配问题），使用 t-avatar/t-cell-group/t-cell/t-icon，渐变色头部+KOC卡片，mock 用户/订单/KOC 数据
- 内容聚焦果蔬：标题统一「鲜到家 · 果蔬」。首页 6 个纯果蔬分类（时令鲜果/热带水果/浆果类/叶菜类/根茎类/茄果类），推荐 6 款果蔬。分类页 6 大类（时令鲜果/进口水果/热带水果/有机蔬菜/根茎瓜果/菌菇豆类），移除全部肉禽蛋/海鲜/乳品/粮油/冻品/零食。购物车/订单 mock 全部果蔬化
- 移除全部 CDN 图标依赖：13 处 t-icon 全部替换为 emoji + Unicode 本地字符（📍▾⏰›🛒🎫⭐💬ℹ️），移除 app.json 全局 t-icon 注册及 home/category/mine 页面注册，t-cell/t-avatar 替换为纯 CSS 实现，所有在用 TDesign 组件（search/tag/image/skeleton/button/stepper/progress/swipe-cell/empty/tabs）均零 t-icon 内部依赖，确认无误
- 订单页 UI 优化：卡片圆角 12→16rpx + 阴影 + 按压缩放反馈，商品缩略图 80→100rpx，价格统一橙红粗体，包裹标签去编号直接显示供应商名，物流标签圆角加粗，加载动画用 3 圆点脉冲替代纯文字，mock 订单图片路径补全
