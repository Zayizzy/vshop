// 小程序全局常量
module.exports = {
  // 导航栏高度偏移量（状态栏高度 + 导航栏内容区）
  NAV_HEIGHT_OFFSET: 44,

  // 截单时间（小时）
  CUTOFF_HOUR: 20,

  // 免配送费门槛（元）
  FREE_SHIPPING_THRESHOLD: 39,

  // 分页默认值
  DEFAULT_PAGE_SIZE: 10,
  LARGE_PAGE_SIZE: 50,
}
